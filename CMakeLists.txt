# Copyright (c) BBP/EPFL 2011-2014 ahmet.bilgili@epfl.ch
# All rights reserved. Do not distribute without further notice.

# Never mind the [] sections. They are only here for doxygen.
# Remove them in your project.

cmake_minimum_required(VERSION 2.8)
project(asyncio)
list(APPEND CMAKE_MODULE_PATH ${CMAKE_SOURCE_DIR}/CMake
  ${CMAKE_SOURCE_DIR}/CMake/common)
include(GitExternal)

set(LAST_RELEASE 1.0.0)
set(VERSION_MAJOR "1")
set(VERSION_MINOR "0")
set(VERSION_PATCH "0")
set(VERSION_ABI 1)

set(ASYNCIO_DESCRIPTION "Async IO project based on futures")
set(COMMON_PROJECT_DOMAIN ch.epfl.bluebrain)
include(Common)
include(FindPackages) # Generated by Buildyard and checked in

add_subdirectory(asyncio)
add_subdirectory(tests)

include(CPackConfig)
include(DoxygenRule)
